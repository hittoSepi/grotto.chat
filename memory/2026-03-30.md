# 2026-03-30 - Safe Native Inline Graphics V2

## Implemented
- Completed safe main-thread native inline graphics compositor v2 in `grotto-chat-client`.
- Added compositor protocol API so layout/backend selection and compositor commit path use the same source of truth:
  - `terminal_inline_protocol()`
  - `terminal_inline_protocol_for_compositor()`
- Removed env-flag dependency (`GROTTO_EXPERIMENTAL_INLINE_NATIVE_IMAGES`) from inline compositor runtime gating.
- Inline native graphics now follows config + capability policy:
  - enabled in `preview.terminal_graphics = "auto"` when protocol is kitty or sixel
  - disabled in `off` and `viewer-only`
- Implemented backend-specific compositor commit + diff logic for both kitty and sixel.
- Ensured layer clear behavior on backend switches, native disable, empty native frame, and full invalidation.
- Explicitly kept iTerm2 out of compositor inline path (viewer path remains separate).

## Files Updated
- `grotto-chat-client/src/ui/terminal_image.hpp`
- `grotto-chat-client/src/ui/terminal_image.cpp`
- `grotto-chat-client/src/ui/graphics_layout.cpp`
- `grotto-chat-client/src/ui/graphics_compositor.cpp`
- `grotto-chat-client/TODOS.md`

## Verification
- Build:
  - `cmake -S grotto-chat-client -B build/grotto-client-graphics -DCMAKE_TOOLCHAIN_FILE=/home/hitto/vcpkg/scripts/buildsystems/vcpkg.cmake`
  - `cmake --build build/grotto-client-graphics --config Debug --target grotto-client test-config test-input-line`
- Tests:
  - `ctest --test-dir build/grotto-client-graphics -C Debug --output-on-failure`
  - result: 2/2 passed
- Manual runtime checks reported by user:
  - inline preview rendering: green
  - scroll/resize stability: green
  - channel switch cleanup behavior: green
  - mode checks (`viewer-only` / `off`): green

